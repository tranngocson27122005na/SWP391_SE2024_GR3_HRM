<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrm.persistence.mapper.LeaveBalanceMapper">
  <resultMap id="BaseResultMap" type="com.hrm.persistence.entity.LeaveBalance">
    <id column="balance_id" jdbcType="INTEGER" property="balanceId" />
    <result column="employee_id" jdbcType="INTEGER" property="employeeId" />
    <result column="year" jdbcType="INTEGER" property="year" />
    <result column="entitled_days" jdbcType="DECIMAL" property="entitledDays" />
    <result column="taken_days" jdbcType="DECIMAL" property="takenDays" />
    <result column="remaining_days" jdbcType="DECIMAL" property="remainingDays" />
    <result column="status" jdbcType="TINYINT" property="status" />
    <result column="created_at" jdbcType="TIMESTAMP" property="createdAt" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from leave_balance
    where balance_id = #{balanceId,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.hrm.persistence.entity.LeaveBalance">
    insert into leave_balance (balance_id, employee_id, year, 
      entitled_days, taken_days, remaining_days, 
      status, created_at)
    values (#{balanceId,jdbcType=INTEGER}, #{employeeId,jdbcType=INTEGER}, #{year,jdbcType=INTEGER}, 
      #{entitledDays,jdbcType=DECIMAL}, #{takenDays,jdbcType=DECIMAL}, #{remainingDays,jdbcType=DECIMAL}, 
      #{status,jdbcType=TINYINT}, #{createdAt,jdbcType=TIMESTAMP})
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.hrm.persistence.entity.LeaveBalance">
    update leave_balance
    set employee_id = #{employeeId,jdbcType=INTEGER},
      year = #{year,jdbcType=INTEGER},
      entitled_days = #{entitledDays,jdbcType=DECIMAL},
      taken_days = #{takenDays,jdbcType=DECIMAL},
      remaining_days = #{remainingDays,jdbcType=DECIMAL},
      status = #{status,jdbcType=TINYINT},
      created_at = #{createdAt,jdbcType=TIMESTAMP}
    where balance_id = #{balanceId,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select balance_id, employee_id, year, entitled_days, taken_days, remaining_days, 
    status, created_at
    from leave_balance
    where balance_id = #{balanceId,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select balance_id, employee_id, year, entitled_days, taken_days, remaining_days, 
    status, created_at
    from leave_balance
  </select>
</mapper>